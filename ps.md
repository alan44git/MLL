# Machine Learning Problems

## Classification

1. **Decision Tree** — Dataset: `madfhantr.csv`  
   Automate loan eligibility for Dream Housing Finance using customer attributes; identify eligible customer segments.

2. **Naïve Bayes** — Dataset: `NaiveBayes.csv`  
   Implement probabilistic classifier and evaluate performance.

18. **Naïve Bayes** — Dataset: `pima-indians-diabetes.csv`  
   Implement probabilistic classifier and evaluate performance.

19. **Naïve Bayes** — Dataset: `Social_Network_Ads.csv`  
   Implement probabilistic classifier and evaluate performance.

20. **Naïve Bayes** — Dataset: `Social_Network_Ads.csv`  
   Implement probabilistic classifier and evaluate performance.

21. **Decision Tree** — Dataset: `pima-indians-diabetes.csv`  
   Create and evaluate a decision tree; test on a random sample.

22. **SVM** — Dataset: `test.csv`  
   Classify Mobile Price Range using SVM and compare accuracy.

23. **SVM** — Dataset: `UniversalBank.csv`  
   Implement SVM and compare accuracy.

24. **SVM** — Dataset: `user_behavior_dataset.csv`  
   Implement SVM.

25. **SVM** — Dataset: `bank_transactions_data_2.csv`  
   Apply SVM with kernels: Linear and Polynomial; compare accuracy.


## Clustering

1. **K-Means** — Dataset: `Cities_r2.csv`  
   Preprocess if needed; cluster based on `total_graduates` to find customer groups.

4. **Hierarchical** — Dataset: `Cities_r2.csv`  
   Preprocess if needed; hierarchical clustering based on `effective_literacy_rate_total` to find customer groups.

5. **K-Means** — Dataset: `Cities_r2.csv`  
   Preprocess if needed; cluster based on `effective_literacy_rate_total` to find customer groups.

6. **Hierarchical** — Dataset: `hitters.csv`  
   Preprocess if needed; hierarchical clustering based on `CRuns` to find player groups.

7. **K-Means** — Dataset: `Social_Network_Ads.csv`  
   Preprocess if needed; K-Means on `EstimatedSalary` to group users.

8. **Hierarchical** — Dataset: `50_Startups.csv`  
   Preprocess if needed; hierarchical clustering on `PROFIT` to group startups.

11. **K-Means (Manual)** — Points `P1..P8`  
   Initialize `m1 = P1` and `m2 = P8`; find cluster of `P6`, population around `m2`, and updated `m1` and `m2`.


## Regression

1. **Simple Linear Regression** — Dataset: `diabetes.csv`  
   Preprocess if needed; use one feature to train/test; compute coefficients, RSS, and R².

10. **Simple Linear Regression** — Dataset: `1.01. Simple linear regression`  
   Model relationship between SAT and GPA; show regression results and line.

12. **Simple Linear Regression** — Dataset: `advertising.csv`  
   Predict sales from TV ad spend; show regression results and line.

13. **Simple Linear Regression** — Dataset: `advertising.csv`  
   Predict sales from Radio ad spend; show regression results and line.

14. **Simple Linear Regression** — Dataset: `advertising.csv`  
   Predict sales from Newspaper ad spend; show regression results and line.


## Market Basket Analysis

1. **Apriori** — Dataset: `Order1.csv`  
   Preprocess, build transactions, train Apriori, visualize rules.

16. **Apriori** — Dataset: `Order2.csv`  
   (7501 transactions, no header) Preprocess, build transactions, train Apriori, visualize rules.

17. **Apriori** — Dataset: `Order3.csv`  
   (Bakery; 20507 rows, 5 columns) Preprocess, build transactions, train Apriori, visualize rules.
